# Compiler files
cache/
out/

# Broadcast / deployment artifacts are ignored by default — Foundry writes the deployer
# private key into run-latest.json (and cache/), so these must never be committed.
# Only the repository's own checked-in broadcast files are kept.
/broadcast/

# Docs
docs/

# Dotenv file
.env

# Node modules
node_modules/
# EigenLayer middleware (reference only; consumer uses ABI-faithful local interface)
/lib/eigenlayer-middleware/
